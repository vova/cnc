<%= form_tag  do %>
  <p>Line name <%= select :train, :line_name, options_for_select(['red', 'green', 'blue']) %></p>
  <p>Initial Travelled Distance <%= text_field :train, :travelled_distance %></p>
  <p><%= submit_tag %></p>
<% end %>
